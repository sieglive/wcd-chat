import { AvatorDirective } from './avator.directive';

describe('AvatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AvatorDirective();
    expect(directive).toBeTruthy();
  });
});
